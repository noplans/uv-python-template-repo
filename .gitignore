# --- Python / uv ---
# uv が作成する仮想環境
.venv/

# Python のコンパイル済みキャッシュ
__pycache__/
*.py[cod]
*$py.class

# ツール固有のキャッシュ
.ruff_cache/
.mypy_cache/
.pytest_cache/
.pyre/

# --- Dev Container / Docker ---
# コンテナビルド時の一時的なコンテキストなどは無視（通常は不要）
# もしローカルで docker-compose.override.yml を使うなら除外
# docker-compose.override.yml

# --- 環境変数・機密情報 ---
.env
.env.local
.env.*.local

# --- OS / IDE ---
.DS_Store
Thumbs.db

# VS Code の個人設定（プロジェクト共通設定以外）
.vscode/
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
